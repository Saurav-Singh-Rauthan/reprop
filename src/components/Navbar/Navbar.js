import React from "react";

import NavbarDesk from "./NavbarDesk/NavbarDesk";

const Navbar = (props) => {
  return (
    <React.Fragment>
      <NavbarDesk />
    </React.Fragment>
  );
};

export default Navbar;
